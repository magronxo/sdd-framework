# verify_feat-041_2026-04-10

feature_id: feat-041
date (UTC): 2026-04-10T21:51:35Z
environment_mode: manual-runtime (windows)
verification_result: PARTIAL

## INVOCATIONS
- engine: sdd-verify
- notes: Manual runtime verification. Windows ACL behavior is nondeterministic unless api-server runs under a restricted user token.

## EVIDENCE
### Preconditions
- Auth token: Authorization: Bearer dev-secret-change-me
- Endpoint under test: GET /api/v1/files?root=<path>&depth=1

### Manual test harness
- Script: 04_tools/manual-tests/test-filetree-acl.ps1
- Env: $env:AGENTICOS_AUTH_TOKEN="dev-secret-change-me"

### Observations
- SDT-02 (control): PASS
  - Observed: HTTP 200 with JSON tree including children (file1.txt, file2.txt)
- SDT-01 (permission denied -> 403): NOT VERIFIED
  - Observed: HTTP 200 on directory with icacls deny RX; response includes root directory node without children.
  - Attempted gold test: runas /user:.\acltest ...
  - Blocker: runas fails with "ERROR DE RUNAS: no se puede adquirir la contraseña de usuario".

## VERDICT
- verdict: PARTIAL
- reasons:
  1) Control case (SDT-02) passes end-to-end in runtime.
  2) Permission denied mapping (SDT-01) cannot be validated deterministically due to inability to run api-server under a restricted user token.
  3) Current runtime evidence for A1 shows HTTP 200, not HTTP 403.
- next_action:
  1) Gold test via Task Scheduler / Windows Service to run api-server as restricted user (acltest).
  2) If A1 still returns HTTP 200 under restricted token, reopen feat-041 for a code fix (no silent ignore / explicit 403 mapping).
